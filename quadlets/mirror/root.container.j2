[Container]
AutoUpdate=registry
ContainerName=mirror
HostName=root.mirror
Image={{ mirror_image }}
LogDriver=journald
Network=proxy.network
Timezone=local
Volume={{ mirror_root_volume }}:/mirror:z,ro

[Service]
Restart=always

[Install]
WantedBy=default.target
