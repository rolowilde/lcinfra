[Container]
AutoUpdate=registry
ContainerName=grafana
Image={{ grafana_image }}
LogDriver=journald
Network=metric.network
PublishPort=3000:3000
Timezone=local
Volume={{ grafana_data_volume }}:/var/lib/grafana:Z

[Service]
Restart=always
TimeoutStartSec=900

[Install]
WantedBy=default.target
