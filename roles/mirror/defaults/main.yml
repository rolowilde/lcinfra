---
mirror_root: /var/mirror
mirror_list: [root, archlinux]

mirror_archlinux:
  config:
    # proto and trailing slash already in the template
    source_url: mirror.accum.se/mirror/archlinux
    rsync_opts: # excludes, etc..
  schedule: '*/33 * * * *'
